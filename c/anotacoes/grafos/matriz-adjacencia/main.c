#include <stdio.h>
#include <stdlib.h>

// Recomendado para grafos densos, i.e, mais arestas do que vertices (consome menos memoria do que listas de adjacencia)

// Buscar a existencia de arestas (relacao) entre dois vertices (basta apenas acessar i, j e verificar)
// Verificar os predecessores do vertice eh simples, basta verificar a sua coluna

